# 第一阶段学习计划

本计划规定**学习流程和记录方式**，不假设、不虚构具体课程章节或视频编号。
实际课程列表以我观看的真实课程为准，随学习进度在 [indexes/timestamps.md](indexes/timestamps.md)
和 [lessons/](lessons/) 中逐步建立。

## 每节课的学习流程

1. **观看前**：新建一份 `raw-notes/video-{course-number}-{short-topic}.md`（或先用无编号占位名称，事后重命名），使用 [templates/raw-note-template.md](templates/raw-note-template.md)。
2. **观看中**：
   - 正常速度观看，优先理解，其次记录。
   - 遇到重要概念、Setup、Brooks 明确强调的规则，或 Brooks 指向图表某处时，快速记录粗略时间戳 + 几个关键词。
   - 不必写完整句子，不必立即分类或归档。
   - 需要截图时，按命名规则保存到 `screenshots/`，在笔记中记录文件名。
   - 遇到不理解的地方，直接记录疑问，不要停下来查资料。
3. **观看后（同一天内）**：
   - 快速补充刚才来不及写完的记录，仍保持原始笔记的"原始性"（不做整理加工）。
   - 不删除、不重写已经写下的内容；如需修正，另起一行标注更正，保留原文。

## 每个模块结束后的复习流程

1. 通读该模块下所有 `raw-notes/` 和已整理的 `lessons/`。
2. 让 Copilot 基于这些文件在 `reviews/` 生成一份复习草稿（`draft` 状态），格式为**考试式问答**：
   - 围绕本模块的概念、Setup、判断原则，生成一批问题（不写答案在题目旁边），让我自己先尝试回答。
   - 每道题标注对应的概念/Setup/来源课程时间戳，便于回答后对照。
   - 参考答案统一放在文档末尾的"参考答案"区域（使用 `<details>` 折叠，避免看题时被剧透）。
   - 仍需列出本模块尚未解决的疑问清单（链接到 `questions/`），以及 Copilot 发现的笔记间矛盾之处（只列出，不裁决）。
3. 我先尝试独立回答问题，再展开参考答案核对。
4. 我确认、修改或标记需要在后续课程中留意的内容。
5. 确认后的复习文档状态更新为 `confirmed`；未确认部分保持 `needs-review`。

## 第一阶段完成标准

第一阶段结束时，项目应当具备：

- [ ] 每节课程的原始笔记（`raw-notes/`）
- [ ] 每节课程的整理笔记（`lessons/`）
- [ ] 可追溯到具体视频和时间戳的重要概念（`glossary/`，通过 `indexes/concepts.md` 索引）
- [ ] 一批有说明的正例、反例和模糊案例（`examples/positive|negative|ambiguous`）
- [ ] 未解决问题清单（`questions/`，通过 `indexes/unresolved-questions.md` 索引）
- [ ] 一些不包含后见之明的 Market Replay 记录
- [ ] 一份逐渐完善的概念词典（`glossary/`）
- [ ] 清楚区分来源、个人理解和 AI 推导（每份文件的状态/来源标签完整）
- [ ] 足够结构化的数据，供第二阶段的视频 AI 和知识提取程序使用

## 哪些信息必须记录

- 视频名称/编号、章节、粗略时间戳（即使只精确到分钟）。
- Brooks 明确说出的规则、术语、判断标准（标记 `source-explicit`）。
- Brooks 用鼠标/画笔指向图表某根 K 线或区域时的说明。
- 任何让我感到"这和我之前理解的不一样"的地方。
- 观看时产生的疑问，即使当下无法展开。
- 截图文件名及其对应的时间戳、上下文。

## 哪些信息不必在第一遍过度整理

- 不必第一遍就给概念分配正式 `PA-CONCEPT-XXXX` ID 或归档到 `glossary/`。
- 不必第一遍就判断某个案例是 positive/negative/ambiguous，可以先粗略记录，模块复习时再分类。
- 不必第一遍就写完整段落式总结，关键词 + 简短理解即可。
- 不必第一遍就解决所有疑问，未解决的先记录到 `raw-notes/`，模块复习时再迁移到 `questions/`。
- 不必强行让术语与其他教材/常见技术分析概念对应，避免把通用知识误归因于 Brooks。

## 如何避免为了做笔记而影响课程理解

- 原始笔记的目标是"够用于回忆"，不是"完整无缺"。
- 允许笔记简陋、跳跃、不完整——这是 `raw-notes/` 的正常状态。
- 如果发现自己因为记笔记而跟不上视频，优先暂停视频，而不是压缩理解时间硬记笔记。
- 整理（进入 `lessons/`）是课后的工作，不要在观看时尝试同步完成整理。
- 疑问不必当场解答，记录下来即可，留到复习或后续课程验证。
