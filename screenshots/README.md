# screenshots/

保存按视频编号和时间戳命名的图表截图。

## 命名规则

```
V{course-number}_{HH-MM-SS}_{concept}_{type}.png
```

`type` 取值：`positive`、`negative`、`ambiguous`、`pointer`、`needs-review`。

示例：`V01_00-12-34_high-2_positive.png`

所有引用截图的 Markdown 文件必须使用**相对路径**链接到本目录下的文件。
